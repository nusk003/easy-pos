import { CheckboxWrapper } from './form-checkbox-wrapper.component';
import { Provider } from './form-provider.component';
import { Section } from './form-section.component';
import { Submit } from './form-submit.component';
import { ModalWrapper } from './modal-wrapper.component';

export const Form = {
  ModalWrapper,
  Provider,
  Section,
  Submit,
  CheckboxWrapper,
};
